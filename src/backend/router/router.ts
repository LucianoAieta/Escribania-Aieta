import express, { Request, Response } from 'express';
import { CreateClient } from '../app/client/app/create/createClient';
import { loadClients } from '../app/client/app/load/loadClients';
const router = express.Router();

module.exports = router;
